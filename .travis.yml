language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-webapplate' ]; then cd .. && eval "mv $currentfolder generator-webapplate" && cd generator-webapplate; fi

